"use strict";

import Decimal from "@jsamr/counter-style/presets/decimal";
import Disc from "@jsamr/counter-style/presets/disc";
import { MarkedListItem, useMarkedList } from "@jsamr/react-native-li";
import React, { Fragment, memo } from "react";
import { jsx as _jsx } from "react/jsx-runtime";
const MDList = ({
  ordered,
  li,
  listStyle,
  textStyle,
  startIndex
}) => {
  const listProps = useMarkedList({
    counterRenderer: ordered ? Decimal : Disc,
    startIndex: startIndex,
    markerTextStyle: textStyle,
    markerBoxStyle: listStyle,
    length: li.length
  });
  return /*#__PURE__*/_jsx(Fragment, {
    children: li.map((node, index) => /*#__PURE__*/_jsx(MarkedListItem, {
      index: index,
      ...listProps,
      children: node
    }, index))
  });
};
export default /*#__PURE__*/memo(MDList);
//# sourceMappingURL=MDList.js.map