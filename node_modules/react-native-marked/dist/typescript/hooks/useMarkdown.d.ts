import { type Tokenizer } from "marked";
import { type ReactNode } from "react";
import type { ColorSchemeName } from "react-native";
import type { RendererInterface } from "../lib/types";
import type { MarkedStyles, UserTheme } from "./../theme/types";
export interface useMarkdownHookOptions {
    colorScheme?: ColorSchemeName;
    renderer?: RendererInterface;
    theme?: UserTheme;
    styles?: MarkedStyles;
    baseUrl?: string;
    tokenizer?: Tokenizer;
}
declare const useMarkdown: (value: string, options?: useMarkdownHookOptions) => ReactNode[];
export default useMarkdown;
//# sourceMappingURL=useMarkdown.d.ts.map